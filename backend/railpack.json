{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "...",
        "cd admin-panel && npm ci && npm run build",
        "cp -r public/assets/uploads storage/seed-uploads"
      ]
    }
  }
}
